---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: checkpoints.apps.crsc.io
spec:
  group: apps.crsc.io
  names:
    kind: Checkpoint
    listKind: CheckpointList
    plural: checkpoints
    singular: checkpoint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Checkpoint is the Schema for the checkpoints API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CheckpointSpec defines the desired state of Checkpoint
            properties:
              containerName:
                description: ContainerName is the name of the container to target
                  the checkpoint.
                type: string
              interval:
                description: Interval is the interval in seconds to perform checkpoints.
                type: integer
              podIp:
                description: PodIP is the IP of the Pod to make the calls for checkpoint.
                type: string
            required:
            - containerName
            - interval
            - podIp
            type: object
          status:
            description: CheckpointStatus defines the observed state of Checkpoint
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
